<%= @task.title %>
<%= @task.details %>
<%= @task.completed %>
<%= link_to 'edit your task', edit_task_path(@task) %>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
